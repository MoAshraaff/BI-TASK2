# Build context is the repo root for both api/ and app/ images (see
# docker-compose.yml), so this one file covers both. Everything here is
# either huge, platform-specific, or something the container builds itself.
**/node_modules
**/.angular
**/dist
**/bin
**/obj
**/*.user
.git
.claude
